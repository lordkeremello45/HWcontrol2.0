# macOS Validation

The macOS release path is validated on a native macOS runner for Apple Silicon.

Validation covers the native build and application/package path. The target is macOS 14+ Apple Silicon.

A successful build validation should not be interpreted as Developer ID signing or Apple notarization unless those credentials and verification steps are actually present.
