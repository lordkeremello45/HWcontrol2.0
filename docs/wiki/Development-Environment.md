# Development Environment

Recommended toolchain:

- Git
- CMake 3.16+
- C++17 compiler
- Go 1.22
- Flutter matching the repository CI version
- platform-native SDK/tooling for the target package

Read the CI workflow before introducing a new dependency so local and release builds remain aligned.
